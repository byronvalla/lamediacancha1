# LaMediaCancha V2 (Multipágina estilo TNT)
Abrir local: VS Code + Live Server (index.html)

Netlify (si el repo tiene carpeta lamediacancha/):
- Publish directory: lamediacancha

Panel /admin:
- Netlify -> Identity (Enable) -> Git Gateway (Enable) -> Invite user -> /admin
